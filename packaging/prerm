#!/bin/sh
set -eu
if [ "${1:-}" = remove ]; then
  systemctl stop family-music.service family-music-worker.service >/dev/null 2>&1 || true
  systemctl disable family-music.service family-music-worker.service >/dev/null 2>&1 || true
fi
exit 0
